# Prisma Migration Structure Placeholder

This directory will contain Prisma migration files for the PostgreSQL database.

- **Prisma** is the planned ORM for database access and migrations.
- **PostgreSQL** is the planned database engine.
- **Migrations are not implemented yet.**
- No `schema.prisma` exists yet (will be added in a future sprint).
- When implemented, migration files will be created in the `migrations/` subdirectory.
- The `DATABASE_URL` environment variable will be required when real persistence is enabled.
